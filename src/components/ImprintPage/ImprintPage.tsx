import styles from './ImprintPage.module.scss';

function ImprintPage() {
	return (
		<main className={styles.main}>
			<section className={styles.imprintPage}>
				<h1>PAGE HEADER</h1>
			</section>
		</main>
	);
}

export default ImprintPage;
